# PostgreSQL credentials used by docker-compose.yml
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kopabridge

# If you want to run the API container with a local .env file,
# copy apps/api/.env.example to apps/api/.env and configure those values.
